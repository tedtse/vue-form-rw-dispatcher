# TimePickerDispatcher

Element Plus 的 `el-time-picker` 组件分发器，添加了只读功能。

## 基本用法

:::demo

time-picker-dispatcher/basic

:::

## 禁用状态

:::demo

time-picker-dispatcher/disabled

:::

## 限制时间选择范围

通过 `disabledHours`，`disabledMinutes` 和 `disabledSeconds` 限制可选时间范围。

:::demo

time-picker-dispatcher/basic-range

:::

## 任意时间范围

添加 `is-range` 属性即可选择时间范围。 同样支持 `arrow-control` 属性。

:::demo

time-picker-dispatcher/range

:::
