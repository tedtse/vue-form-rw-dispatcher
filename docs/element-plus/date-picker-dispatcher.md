# DatePickerDispatcher

Element Plus 的 `el-date-picker` 组件分发器，添加了只读功能。

## 选择某一天

以”日“为基本单位，基础的日期选择控件

:::demo

date-picker-dispatcher/enter-date

:::

## 其他日期单位 ​

通过扩展基础的日期选择，可以选择周、月、年或多个日期

:::demo

date-picker-dispatcher/other-measurements

:::

## 选择一段时间

:::demo

date-picker-dispatcher/date-range

:::

## 设置自定义前缀的内容​

前缀内容可以被自定义。

当你从其他vue组件或由渲染函数生成的组件中导入组件时, 你可以设置 `prefix-icon` 属性来定制前缀内容

:::demo

date-picker-dispatcher/custom-prefix-icon

:::