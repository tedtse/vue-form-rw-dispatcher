# RadioDispatcher

Element Plus 的 `el-radio-group` 组件分发器，添加了只读功能。

## 基本用法

:::demo

radio-dispatcher/basic-usage

:::

## 禁用状态

:::demo

radio-dispatcher/disabled

:::

## Options 属性

:::demo

radio-dispatcher/options

:::

## 单选按钮

:::demo

radio-dispatcher/button-fill-textcolor

:::

## Slots

使用 `slots` 中的 `${Config.namespace}Reader`, `${Config.namespace}Writer` 覆盖读写状态渲染函数。

:::demo

radio-dispatcher/slots

:::
